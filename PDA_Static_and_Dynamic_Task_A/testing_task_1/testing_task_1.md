### Testing task 1:

# Carry out static testing on the code below.
# Comment on any errors that you see below.
```ruby

require_relative('card.rb')
class CardGame


  def checkforAce(card)
    if card.value = 1
      return true
    else
      return false
    end
  end
# change the = to ==
# change method name to snake_case


  dif highest_card(card1 card2)
  if card1.value > card2.value
    return card.name
  else
    card2
  end
end
end

# Amend dif to def
# Add a comma between card1 and card2 in the brackets
# Change return card.name to return card1.suit()
# Change else card2 to else return card2.suit()
# Remove the extra end

def self.cards_total(cards)
  total
  for card in cards
    total += card.value
    return "You have a total of" + total
  end
end

# Change total to equal zero i.e. needs a starting point/counter to add to
# Change the +total to a string - interpolation
# Add an extra end for the class
```
